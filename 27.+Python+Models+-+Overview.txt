

- Link to the documentation about dbt Python models: https://docs.getdbt.com/docs/build/python-models


- Link to the python packages available in Snowflake: https://repo.anaconda.com/pkgs/snowflake/


- Also within Snowflake, you can see the packages available doing:

	select
	*
	from BTC.INFORMATION_SCHEMA.PACKAGES
	
	limit 10;



 - Code for python model stg_btc_outputs.py:
 
***

import pandas


def model(dbt, session):

	dbt.config(materialized="table", packages=["pandas"])
	
	df = dbt.ref("stg_btc").to_pandas()
	
	return df


***